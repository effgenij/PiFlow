name: piflow-spec
version: 1
description: PiFlow SDD workflow — proposal → specs → design → adr → tasks → superpowers apply (ADRs persist at <repo>/adr/)

artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: |
      Create the proposal document that establishes WHY this change is needed.

      Sections:
      - **Why**: 1-2 sentences on the problem or opportunity. What problem does this solve? Why now?
      - **What Changes**: Bullet list of changes. Be specific about new capabilities, modifications, or removals. Mark breaking changes with **BREAKING**.
      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names (e.g., `user-auth`, `data-export`).
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Check `openspec/specs/` for existing spec names. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.

      IMPORTANT: The Capabilities section is critical. It creates the contract between
      proposal and specs phases. Research existing specs before filling this in.
      Each capability listed here will need a corresponding spec file.

      Keep it concise (1-2 pages). Focus on the "why" not the "how" -
      implementation details belong in design.md.

      Durable architectural decisions worth carrying across changes are
      distilled into ADR files at <repo>/adr/ by a later step (after design).

      This is the foundation - specs, design, adr, and tasks all build on this.
    requires: []

  - id: specs
    generates: "specs/**/*.md"
    description: Detailed specifications for the change
    template: spec.md
    instruction: |
      Create specification files that define WHAT the system should do.

      Create one spec file per capability listed in the proposal's Capabilities section.
      - New capabilities: use the exact kebab-case name from the proposal (specs/<capability>/spec.md).
      - Modified capabilities: use the existing spec folder name from openspec/specs/<capability>/ when creating the delta spec at specs/<capability>/spec.md.

      Delta operations (use ## headers):
      - **ADDED Requirements**: New capabilities
      - **MODIFIED Requirements**: Changed behavior - MUST include full updated content
      - **REMOVED Requirements**: Deprecated features - MUST include **Reason** and **Migration**
      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format

      Format requirements:
      - Each requirement: `### Requirement: <name>` followed by description
      - Use SHALL/MUST for normative requirements (avoid should/may)
      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format
      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3 hashtags or bullets will fail silently.
      - Every requirement MUST have at least one scenario.

      MODIFIED requirements workflow:
      1. Locate the existing requirement in openspec/specs/<capability>/spec.md
      2. Copy the ENTIRE requirement block (from `### Requirement:` through all scenarios)
      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior
      4. Ensure header text matches exactly (whitespace-insensitive)

      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal

  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: |
      Create the design document that explains HOW to implement the change.

      When to include design.md (create only if any apply):
      - Cross-cutting change (multiple services/modules) or new architectural pattern
      - New external dependency or significant data model changes
      - Security, performance, or migration complexity
      - Ambiguity that benefits from technical decisions before coding

      Prior decisions (read existing ADRs first):
      Before writing, list every file in <repo>/adr/. Build the supersession
      graph from each ADR's Supersedes field — an ADR is no longer in force if
      a later ADR's Supersedes points at it. Only currently-in-force ADRs
      (accepted, not superseded) constrain this design. New design choices
      MUST be coherent with the in-force set. If a current ADR needs to be
      revisited, do NOT modify it — flag it under Open Questions and let the
      adr step record a new superseding ADR.

      Sections:
      - **Context**: Background, current state, constraints, stakeholders
      - **Goals / Non-Goals**: What this design achieves and explicitly excludes
      - **Decisions**: Key technical choices with rationale (why X over Y?). Include alternatives considered for each decision.
      - **Risks / Trade-offs**: Known limitations, things that could go wrong. Format: [Risk] → Mitigation
      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)
      - **Open Questions**: Outstanding decisions or unknowns to resolve.
        Include any in-force ADRs that this design suggests revisiting — the
        adr step will record the supersession.

      Focus on architecture and approach, not line-by-line implementation.
      Reference the proposal for motivation and specs for requirements.

      Good design docs explain the "why" behind technical decisions.
    requires:
      - proposal

  - id: adr
    generates: "../../../adr/*.md"
    description: Architecture Decision Records distilled from design for future reference
    template: adr.md
    instruction: |
      Distill significant decisions from design.md into Architecture Decision
      Records that live in <repo>/adr/ and persist across changes. The path
      <repo>/adr/ means the top-level adr/ folder beside openspec/, not any
      folder inside openspec/. ADR files MUST be written outside openspec/ and
      MUST NOT be written under openspec/changes/<change>/.

      IRON RULE — ADRs are immutable once accepted. NEVER modify an existing
      ADR file. If a decision changes, create a NEW ADR that supersedes the old one.

      Steps:
      1. List all files in <repo>/adr/ to understand existing decisions and
         find the highest sequence number.
      2. For each decision in design.md, evaluate against this bar — it must:
         - Establish a long-term architectural commitment (pattern, technology,
           boundary, contract) — not a tactical implementation detail.
         - Affect future changes beyond this one.
         - Either not already be captured by a currently-in-force ADR, OR
           intentionally diverge from one (in which case the new ADR supersedes it).
      3. For each qualifying decision, create a NEW file at
         <repo>/adr/NNNN-kebab-title.md, where NNNN is 4 digits, one greater
         than the highest existing. Use the adr.md template (MADR-short).
         If this decision replaces a prior in-force ADR, set Status to
         "accepted, supersedes ADR-NNNN", populate the Supersedes field, and
         explain in Context why the prior decision is being revisited.
         Do NOT touch the prior ADR's file — leave it exactly as-is.
      4. If nothing meets the bar, the step still completes as long as
         <repo>/adr/ already contains at least one ADR file.
         Do not invent ADRs just to have something to write.

      Filename convention: NNNN-kebab-title.md (e.g., 0001-use-openspec-for-sdd.md).
      Sequence is monotonic across the whole repo and never reused.
    requires:
      - design

  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: |
      Create the task list that breaks down the implementation work.

      **IMPORTANT: Follow the template below exactly.** The apply phase parses
      checkbox format to track progress. Tasks not using `- [ ]` won't be tracked.

      Guidelines:
      - Group related tasks under ## numbered headings
      - Each task MUST be a checkbox: `- [ ] X.Y Task description`
      - Tasks should be small enough to complete in one session
      - Order tasks by dependency (what must be done first?)

      Example:
      ```
      ## 1. Setup

      - [ ] 1.1 Create new module structure
      - [ ] 1.2 Add dependencies to package.json

      ## 2. Core Implementation

      - [ ] 2.1 Implement data export function
      - [ ] 2.2 Add CSV formatting utilities
      ```

      Reference specs for what to build, design for how to build it, and
      <repo>/adr/ for the durable architectural commitments to honor.
      Each task should be verifiable - you know when it's done.
    requires:
      - specs
      - adr

apply:
  requires: [tasks]
  tracks: tasks.md
  instruction: |
    You are about to implement the change described in this OpenSpec change folder.
    Use the superpowers:executing-plans skill to execute this work.

    ## Context files to load first
    Read all of the following before starting:
    - proposal.md — WHY this change exists
    - specs/**/*.md — WHAT must be built (requirements + scenarios)
    - design.md — HOW to build it (if present)
    - <repo>/adr/*.md — architectural commitments to honor
    - tasks.md — the task checklist to execute

    ## Execution process (superpowers:executing-plans)

    Follow the superpowers:executing-plans skill exactly:

    ### Step 1: Load and Review
    1. Read all context files listed above
    2. Review tasks.md critically — identify any gaps or blockers before starting
    3. If concerns: raise them before proceeding
    4. If no concerns: create a TodoWrite list from tasks.md and proceed

    ### Step 2: Setup Workspace
    - Use superpowers:using-git-worktrees to ensure you are in an isolated worktree
    - The worktree branch name should match the change folder name

    ### Step 3: Execute Tasks
    For each task in tasks.md:
    1. Mark as in_progress
    2. Follow each step exactly as specified
    3. Run verifications as you go (tests, lint, type checks)
    4. Mark as completed only when verified

    STOP immediately if:
    - A blocker or missing dependency arises
    - A test fails and the fix is not obvious
    - An instruction is unclear or contradicts the spec
    Ask for clarification rather than guessing.

    ### Step 4: Complete Development
    After all tasks are complete and verified:
    - Use superpowers:finishing-a-development-branch to finalize the branch
    - Follow that skill to verify tests, present options, execute choice
